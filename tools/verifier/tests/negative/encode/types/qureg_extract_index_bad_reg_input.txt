( version = 0,
  functions = [
    ( name = 0,
      definition = (
        body = (
          sources = [],
          targets = [],
          operations = [
            ( inputs = [],
              outputs = [0],
              instruction = (int = (const32 = 0)) ),
            ( inputs = [],
              outputs = [1],
              instruction = (qubit = (alloc = void)) ),
            ( inputs = [1, 0],
              outputs = [2, 3],
              instruction = (qureg = (extractIndex = void)) ) ] ),
        values = [
          (type = (int = 32)),
          (type = (qubit = void)),
          (type = (qureg = (dynamic = void))),
          (type = (qubit = void)) ] ) ) ],
  strings = ["f"],
  entrypoint = 0,
  tool = "",
  toolVersion = "",
  versionMinor = 2,
  versionPatch = 0 )
